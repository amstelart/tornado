//- Все примеси в этом файле должны начинаться c имени блока (main-box-info)

mixin main-box-info(text, mods)

  //- Принимает:
  //-   text    {string} - текст
  //-   mods    {string} - список модификаторов
  //- Вызов:
        +main-box-info('Текст', 'some-mod')

  -
    // список модификаторов
    var allMods = '';
    if(typeof(mods) !== 'undefined' && mods) {
      var modsList = mods.split(',');
      for (var i = 0; i < modsList.length; i++) {
        allMods = allMods + ' main-box-info--' + modsList[i].trim();
      }
    }

  .main-box-info(class=allMods)&attributes(attributes)
    .main-box-info__inner!= text