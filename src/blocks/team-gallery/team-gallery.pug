//- Все примеси в этом файле должны начинаться c имени блока (team-gallery)

mixin team-gallery(text, mods)

  //- Принимает:
  //-   text    {string} - текст
  //-   mods    {string} - список модификаторов
  //- Вызов:
        +team-gallery('Текст', 'some-mod')

  -
    // список модификаторов
    var allMods = '';
    if(typeof(mods) !== 'undefined' && mods) {
      var modsList = mods.split(',');
      for (var i = 0; i < modsList.length; i++) {
        allMods = allMods + ' team-gallery--' + modsList[i].trim();
      }
    }

  .team-gallery(class=allMods)&attributes(attributes)
    .team-gallery__inner!= text